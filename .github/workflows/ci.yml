name: CI

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  # A new push supersedes the run it interrupted; only the tip of a branch matters.
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    services:
      # Throwaway database so db:setup can apply migrations before the build.
      postgres:
        image: postgres:18
        env:
          POSTGRES_USER: k5
          POSTGRES_PASSWORD: ci-build-only
          POSTGRES_DB: k5_ci
        ports: ['5432:5432']
        options: >-
          --health-cmd "pg_isready -U k5 -d k5_ci"
          --health-interval 5s --health-timeout 5s --health-retries 10
    steps:
      - uses: actions/checkout@v5

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v6
        with:
          # The repo pins >=22.13 because the application uses node:sqlite.
          node-version-file: package.json
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm lint

      - run: pnpm typecheck

      - run: pnpm test

      - name: Build
        # Migrate the service database before parallel page collection.
        # These values are for compilation only; staging secrets stay out of PR builds.
        env:
          DATABASE_URL: postgresql://k5:ci-build-only@127.0.0.1:5432/k5_ci
          BETTER_AUTH_SECRET: ci-build-only-secret-not-used-at-runtime-000000
          BETTER_AUTH_URL: http://localhost:3000
          K5_CREDENTIALS_KEY: Y2ktYnVpbGQtb25seS1rZXktbm90LXVzZWQtYXQtcnQ=
          SENTRY_AUTH_TOKEN: ${{ github.event_name == 'push' && secrets.SENTRY_AUTH_TOKEN || '' }}
          SENTRY_REQUIRE_SOURCEMAPS: ${{ github.event_name == 'push' && 'true' || 'false' }}
          SENTRY_RELEASE: ${{ github.sha }}
          NEXT_PUBLIC_SENTRY_RELEASE: ${{ github.sha }}
          SENTRY_ENABLED: 'false'
          NEXT_PUBLIC_SENTRY_ENABLED: 'false'
        run: |
          pnpm db:setup
          pnpm build

  migrations:
    # Migrations are append-only: editing a file that already ran leaves every existing database
    # on the old schema with no way to notice.
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Refuse edits to applied migrations
        run: |
          base="${{ github.event.pull_request.base.sha }}"
          changed=$(git diff --name-only --diff-filter=MD "$base"...HEAD -- 'apps/web/db/migrations/*.sql')
          if [ -n "$changed" ]; then
            echo "Estas migraÃ§Ãµes jÃ¡ aplicadas foram alteradas ou removidas:"
            echo "$changed"
            echo "Crie uma nova migraÃ§Ã£o em vez de editar uma existente."
            exit 1
          fi
          echo "Nenhuma migraÃ§Ã£o existente foi alterada."
