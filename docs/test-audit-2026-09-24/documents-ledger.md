# Documents, chat, capabilities and feedback audit ledger
Baseline: main 5e6ab96 plus pre-existing Google release changes; baseline-files.json and inventory.json pin exact bytes/results. Read-only discovery: no tests/source edits or execution by this lane. All assigned test bodies and parameter tables read. Candidate production owners, callers and relevant history inspected in full; retained contracts use conservative retention when no stronger proof is demonstrated. See layer plan below for six eligible reductions; each requires owner review before cutover.
CI: .github/workflows/ci.yml runs pnpm lint, pnpm typecheck, pnpm test and configured build. scripts/test-postgres.ts enumerates all tests/*.test.ts, node:test concurrency 4, real temporary PostgreSQL. No explicit registration updates needed. OpenClaw-only commands in supplied policy are unavailable here; root runs equivalent K5 checks.

## tests/agent-approvals.test.ts
- **R** `tests/agent-approvals.test.ts:39` — 'agent autonomy: low-impact writes run at once, with a link to the result'. Agent execution/confirmation boundary: prevents unauthorized or unconfirmed deletion, preserves decision after reload and result links. Keep actual dispatch and persisted outcome, not just declarations.
- **R** `tests/agent-approvals.test.ts:48` — 'agent approvals: deleting waits for Confirmar in the chat, then runs exactly what was proposed'. Agent execution/confirmation boundary: prevents unauthorized or unconfirmed deletion, preserves decision after reload and result links. Keep actual dispatch and persisted outcome, not just declarations.
- **R** `tests/agent-approvals.test.ts:65` — 'agent approvals: cancel changes nothing, and a proposal cannot be reused for another target'. Agent execution/confirmation boundary: prevents unauthorized or unconfirmed deletion, preserves decision after reload and result links. Keep actual dispatch and persisted outcome, not just declarations.
- **R** `tests/agent-approvals.test.ts:79` — 'agent approvals: the interface is not gated, and other people cannot decide'. Agent execution/confirmation boundary: prevents unauthorized or unconfirmed deletion, preserves decision after reload and result links. Keep actual dispatch and persisted outcome, not just declarations.
- **R** `tests/agent-approvals.test.ts:93` — 'agent approvals: only gated actions can be confirmed from the chat'. Agent execution/confirmation boundary: prevents unauthorized or unconfirmed deletion, preserves decision after reload and result links. Keep actual dispatch and persisted outcome, not just declarations.

## tests/agent-instructions.test.ts
- **R** `tests/agent-instructions.test.ts:20` — 'agent instructions: office rules reach every member, personal rules only their author'. Saved rule and prompt boundary: prevents office/personal leakage, unauthorized writes, stale overwrite, off-target instructions and delimiter injection; prompt budget is independently observable.
- **R** `tests/agent-instructions.test.ts:39` — 'agent instructions: roles decide who writes office rules; reviewers keep their own'. Saved rule and prompt boundary: prevents office/personal leakage, unauthorized writes, stale overwrite, off-target instructions and delimiter injection; prompt budget is independently observable.
- **R** `tests/agent-instructions.test.ts:57` — 'agent instructions: stale edits conflict, and disabled or off-target rules stay out of the prompt'. Saved rule and prompt boundary: prevents office/personal leakage, unauthorized writes, stale overwrite, off-target instructions and delimiter injection; prompt budget is independently observable.
- **R** `tests/agent-instructions.test.ts:76` — 'agent instructions: a rule cannot break out of its block, and the enabled budget is enforced'. Saved rule and prompt boundary: prevents office/personal leakage, unauthorized writes, stale overwrite, off-target instructions and delimiter injection; prompt budget is independently observable.

## tests/agent-knowledge.test.ts
- **R** `tests/agent-knowledge.test.ts:33` — 'agent knowledge: always-read text reaches the prompt as data; search documents are only named'. Saved reference and prompt boundary: prevents document/office leakage, duplicate whole-document inclusion, truncation masquerading as complete context and data escaping its prompt block.
- **R** `tests/agent-knowledge.test.ts:54` — 'agent knowledge: a document over the budget is listed for search, never cut'. Saved reference and prompt boundary: prevents document/office leakage, duplicate whole-document inclusion, truncation masquerading as complete context and data escaping its prompt block.
- **R** `tests/agent-knowledge.test.ts:67` — 'agent knowledge: quoted text cannot close its block, and a shared document is read once'. Saved reference and prompt boundary: prevents document/office leakage, duplicate whole-document inclusion, truncation masquerading as complete context and data escaping its prompt block.
- **R** `tests/agent-knowledge.test.ts:78` — 'agent knowledge: scopes, roles and offices are enforced'. Saved reference and prompt boundary: prevents document/office leakage, duplicate whole-document inclusion, truncation masquerading as complete context and data escaping its prompt block.

## tests/agent-profile.test.ts
- **R** `tests/agent-profile.test.ts:29` — 'agent profile: the personal template wins over the office one, and removing it falls back'. Saved template selection boundary: prevents cross-office or deleted templates, unsupported files, unauthorized template changes and broken personal-to-office fallback.
- **R** `tests/agent-profile.test.ts:50` — 'agent profile: only administrators change the office template; reviewers change nothing'. Saved template selection boundary: prevents cross-office or deleted templates, unsupported files, unauthorized template changes and broken personal-to-office fallback.
- **R** `tests/agent-profile.test.ts:61` — 'agent profile: templates stay inside the office and must be Word files'. Saved template selection boundary: prevents cross-office or deleted templates, unsupported files, unauthorized template changes and broken personal-to-office fallback.
- **R** `tests/agent-profile.test.ts:73` — 'agent profile: a template deleted in the Cofre stops applying'. Saved template selection boundary: prevents cross-office or deleted templates, unsupported files, unauthorized template changes and broken personal-to-office fallback.

## tests/ai-providers.test.ts
- **R** `tests/ai-providers.test.ts:9` — "modelFor binds the given key and model id to the provider, without network calls". Provider configuration boundary: prevents credential/model crossover, unsupported router entries, missing published defaults, embedding choices offered as chat and unsupported modalities enabled. Not merely a copied manifest; public/default/adapter contracts justify retention.
- **R** `tests/ai-providers.test.ts:20` — "every supported provider resolves through the model router and is named in the interface". Provider configuration boundary: prevents credential/model crossover, unsupported router entries, missing published defaults, embedding choices offered as chat and unsupported modalities enabled. Not merely a copied manifest; public/default/adapter contracts justify retention.
- **R** `tests/ai-providers.test.ts:30` — "GPT-6 Sol and Luna are available to the platform administrator". Provider configuration boundary: prevents credential/model crossover, unsupported router entries, missing published defaults, embedding choices offered as chat and unsupported modalities enabled. Not merely a copied manifest; public/default/adapter contracts justify retention.
- **R** `tests/ai-providers.test.ts:42` — "Lume owns the default model of every provider, and it is a model the router knows". Provider configuration boundary: prevents credential/model crossover, unsupported router entries, missing published defaults, embedding choices offered as chat and unsupported modalities enabled. Not merely a copied manifest; public/default/adapter contracts justify retention.
- **R** `tests/ai-providers.test.ts:58` — "modalities are read from the model id and fail closed on an unknown one". Provider configuration boundary: prevents credential/model crossover, unsupported router entries, missing published defaults, embedding choices offered as chat and unsupported modalities enabled. Not merely a copied manifest; public/default/adapter contracts justify retention.

## tests/ai-store.test.ts
- **R** `tests/ai-store.test.ts:29` — "chat bootstrap includes owned messages and rejects another user's deep link". Persisted chat/artifact owner or history transition: catches deep-link leakage, write-on-read, message truncation mistakes, lost versions, stale overwrite and non-atomic history. Keep transactional and concurrent probes independently.
- **R** `tests/ai-store.test.ts:49` — "chat bootstrap is read-only for an empty history and opens an older owned deep link". Persisted chat/artifact owner or history transition: catches deep-link leakage, write-on-read, message truncation mistakes, lost versions, stale overwrite and non-atomic history. Keep transactional and concurrent probes independently.
- **R** `tests/ai-store.test.ts:65` — "mergeHistory appends a new user message". Persisted chat/artifact owner or history transition: catches deep-link leakage, write-on-read, message truncation mistakes, lost versions, stale overwrite and non-atomic history. Keep transactional and concurrent probes independently.
- **D** `tests/ai-store.test.ts:71` — "mergeHistory truncates on regenerate without duplicating the user turn". Candidate evidence and exact keeper in the layer plan below.
- **R** `tests/ai-store.test.ts:80` — "mergeHistory replaces and truncates when an earlier message is edited". Persisted chat/artifact owner or history transition: catches deep-link leakage, write-on-read, message truncation mistakes, lost versions, stale overwrite and non-atomic history. Keep transactional and concurrent probes independently.
- **R** `tests/ai-store.test.ts:87` — "conversation, run and artifact access is scoped by office and user". Persisted chat/artifact owner or history transition: catches deep-link leakage, write-on-read, message truncation mistakes, lost versions, stale overwrite and non-atomic history. Keep transactional and concurrent probes independently.
- **R** `tests/ai-store.test.ts:120` — "updateArtifact enforces optimistic version conflicts". Persisted chat/artifact owner or history transition: catches deep-link leakage, write-on-read, message truncation mistakes, lost versions, stale overwrite and non-atomic history. Keep transactional and concurrent probes independently.
- **R** `tests/ai-store.test.ts:145` — "updateArtifact rolls back the artifact when history cannot be recorded". Persisted chat/artifact owner or history transition: catches deep-link leakage, write-on-read, message truncation mistakes, lost versions, stale overwrite and non-atomic history. Keep transactional and concurrent probes independently.
- **R** `tests/ai-store.test.ts:169` — "updateArtifact: autosave bumps the version but records history at most every five minutes". Persisted chat/artifact owner or history transition: catches deep-link leakage, write-on-read, message truncation mistakes, lost versions, stale overwrite and non-atomic history. Keep transactional and concurrent probes independently.
- **R** `tests/ai-store.test.ts:193` — 'updateArtifact: concurrent replacements preserve the autosaved text and reject the loser'. Persisted chat/artifact owner or history transition: catches deep-link leakage, write-on-read, message truncation mistakes, lost versions, stale overwrite and non-atomic history. Keep transactional and concurrent probes independently.

## tests/animation-ssr.test.ts
- **R** `tests/animation-ssr.test.ts:7` — `${component} can be imported for SSR without starting animation timers`. Separate fresh imports of reveal and app-sidebar guard Worker timer restrictions. Each public import can independently start timers, so two executed cases remain.

## tests/annexes.test.ts
- **C** `tests/annexes.test.ts:32` — 'annexes: PJe file names drop accents, cedillas and special characters'. Candidate evidence and exact keeper in the layer plan below.
- **R** `tests/annexes.test.ts:39` — 'annexes: order follows the first citation in the petition, not the scan order'. Reviewed annex PDF boundary: prevents wrong page ranges, citation-order mistakes, invalid inputs and foreign-case documents; real PDF page widths verify copied page identity.
- **R** `tests/annexes.test.ts:60` — 'annexes: generation cuts the reviewed ranges into a new folder, scoped to the office and case'. Reviewed annex PDF boundary: prevents wrong page ranges, citation-order mistakes, invalid inputs and foreign-case documents; real PDF page widths verify copied page identity.
- **R** `tests/annexes.test.ts:85` — 'annexes: planning waits for OCR and a petition, and reviewers cannot run it'. Reviewed annex PDF boundary: prevents wrong page ranges, citation-order mistakes, invalid inputs and foreign-case documents; real PDF page widths verify copied page identity.

## tests/artifact-edits.test.ts
- **R** `tests/artifact-edits.test.ts:12` — 'artifact edits: applied in order, all or nothing, and only on a unique excerpt'. Document-edit boundary: prevents wrong excerpt replacement, partial application, unauthorized cross-conversation edit, stale version, lost history, citation review omission and selected-text prompt injection.
- **R** `tests/artifact-edits.test.ts:39` — 'artifact edits: the agent refines its own document in the same conversation without asking'. Document-edit boundary: prevents wrong excerpt replacement, partial application, unauthorized cross-conversation edit, stale version, lost history, citation review omission and selected-text prompt injection.
- **R** `tests/artifact-edits.test.ts:64` — 'artifact edits: another conversation, a job document or another person needs confirmation or sees nothing'. Document-edit boundary: prevents wrong excerpt replacement, partial application, unauthorized cross-conversation edit, stale version, lost history, citation review omission and selected-text prompt injection.
- **R** `tests/artifact-edits.test.ts:81` — 'artifact edits: the list puts this conversation first, and the tools stay off WebMCP'. Document-edit boundary: prevents wrong excerpt replacement, partial application, unauthorized cross-conversation edit, stale version, lost history, citation review omission and selected-text prompt injection.
- **R** `tests/artifact-edits.test.ts:92` — 'artifact edits: the open document and a selection reach the prompt as data'. Document-edit boundary: prevents wrong excerpt replacement, partial application, unauthorized cross-conversation edit, stale version, lost history, citation review omission and selected-text prompt injection.

## tests/calendar-days.test.ts
- **R** `tests/calendar-days.test.ts:10` — 'calendar markers combine tasks and overlapping meetings without marking an exclusive midnight end'. Calendar-day markers: catches exclusive-midnight off-by-one, month clipping and leap-day mistakes. UI script overlaps ordinary markers but not leap-year clipping; retain both.
- **R** `tests/calendar-days.test.ts:20` — 'calendar markers clip meetings crossing month boundaries and handle leap days'. Calendar-day markers: catches exclusive-midnight off-by-one, month clipping and leap-day mistakes. UI script overlaps ordinary markers but not leap-year clipping; retain both.

## tests/capabilities.test.ts
- **R** `tests/capabilities.test.ts:67` — "capabilities contract: complete catalog and role permissions". Capability/application/adapter boundary: catches authorization, persistence, approval, typed transport failure or publication-contract regressions exercised by this exact scenario. A mocked network is transport isolation, not proof of server delivery.
- **R** `tests/capabilities.test.ts:90` — "authorization: dynamic role check and membership revocation". Capability/application/adapter boundary: catches authorization, persistence, approval, typed transport failure or publication-contract regressions exercised by this exact scenario. A mocked network is transport isolation, not proof of server delivery.
- **R** `tests/capabilities.test.ts:114` — "authorization: a live session is checked by its resolved column". Capability/application/adapter boundary: catches authorization, persistence, approval, typed transport failure or publication-contract regressions exercised by this exact scenario. A mocked network is transport isolation, not proof of server delivery.
- **R** `tests/capabilities.test.ts:128` — "vault service: idempotent case creation, update and deletion with approval". Capability/application/adapter boundary: catches authorization, persistence, approval, typed transport failure or publication-contract regressions exercised by this exact scenario. A mocked network is transport isolation, not proof of server delivery.
- **R** `tests/capabilities.test.ts:167` — "vault deletion clears stale folders when documents move to another case". Capability/application/adapter boundary: catches authorization, persistence, approval, typed transport failure or publication-contract regressions exercised by this exact scenario. A mocked network is transport isolation, not proof of server delivery.
- **R** `tests/capabilities.test.ts:192` — "vault deletion can resume after approval consumption". Capability/application/adapter boundary: catches authorization, persistence, approval, typed transport failure or publication-contract regressions exercised by this exact scenario. A mocked network is transport isolation, not proof of server delivery.
- **R** `tests/capabilities.test.ts:218` — "vault document service: versions, tombstone and office isolation". Capability/application/adapter boundary: catches authorization, persistence, approval, typed transport failure or publication-contract regressions exercised by this exact scenario. A mocked network is transport isolation, not proof of server delivery.
- **R** `tests/capabilities.test.ts:256` — "knowledge engine: hybrid retrieval, source inspection and audit". Capability/application/adapter boundary: catches authorization, persistence, approval, typed transport failure or publication-contract regressions exercised by this exact scenario. A mocked network is transport isolation, not proof of server delivery.
- **R** `tests/capabilities.test.ts:317` — "knowledge search covers ready documents while another selected one is still processing". Capability/application/adapter boundary: catches authorization, persistence, approval, typed transport failure or publication-contract regressions exercised by this exact scenario. A mocked network is transport isolation, not proof of server delivery.
- **D** `tests/capabilities.test.ts:341` — "idempotency: cached execution prevents duplicated writes". Candidate evidence and exact keeper in the layer plan below.
- **R** `tests/capabilities.test.ts:361` — "artifacts service: version history and rollback restoration". Capability/application/adapter boundary: catches authorization, persistence, approval, typed transport failure or publication-contract regressions exercised by this exact scenario. A mocked network is transport isolation, not proof of server delivery.
- **R** `tests/capabilities.test.ts:407` — "conversations service: lifecycle and processing locking". Capability/application/adapter boundary: catches authorization, persistence, approval, typed transport failure or publication-contract regressions exercised by this exact scenario. A mocked network is transport isolation, not proof of server delivery.
- **R** `tests/capabilities.test.ts:435` — "agent tools: mastra tools creation and summary formatting". Capability/application/adapter boundary: catches authorization, persistence, approval, typed transport failure or publication-contract regressions exercised by this exact scenario. A mocked network is transport isolation, not proof of server delivery.
- **R** `tests/capabilities.test.ts:456` — "webmcp: registration adapter handles mock browser modelContext". Capability/application/adapter boundary: catches authorization, persistence, approval, typed transport failure or publication-contract regressions exercised by this exact scenario. A mocked network is transport isolation, not proof of server delivery.
- **R** `tests/capabilities.test.ts:484` — "capability routes: boolean query parameters accept only one exact true or false". Capability/application/adapter boundary: catches authorization, persistence, approval, typed transport failure or publication-contract regressions exercised by this exact scenario. A mocked network is transport isolation, not proof of server delivery.
- **R** `tests/capabilities.test.ts:497` — "approval security: rejects modified target resource or modified input arguments". Capability/application/adapter boundary: catches authorization, persistence, approval, typed transport failure or publication-contract regressions exercised by this exact scenario. A mocked network is transport isolation, not proof of server delivery.
- **R** `tests/capabilities.test.ts:525` — "knowledge engine: vectors fuse with lexical hits and tombstones stay out". Capability/application/adapter boundary: catches authorization, persistence, approval, typed transport failure or publication-contract regressions exercised by this exact scenario. A mocked network is transport isolation, not proof of server delivery.
- **R** `tests/capabilities.test.ts:574` — "knowledge scope: updating sources for a conversation does not duplicate rows". Capability/application/adapter boundary: catches authorization, persistence, approval, typed transport failure or publication-contract regressions exercised by this exact scenario. A mocked network is transport isolation, not proof of server delivery.
- **R** `tests/capabilities.test.ts:595` — "ui service: openResource validates resource access and throws NOT_FOUND on cross-office or invalid IDs". Capability/application/adapter boundary: catches authorization, persistence, approval, typed transport failure or publication-contract regressions exercised by this exact scenario. A mocked network is transport isolation, not proof of server delivery.
- **R** `tests/capabilities.test.ts:615` — "webmcp: every published capability has a route, a schema and typed failures". Capability/application/adapter boundary: catches authorization, persistence, approval, typed transport failure or publication-contract regressions exercised by this exact scenario. A mocked network is transport isolation, not proof of server delivery.
- **R** `tests/capabilities.test.ts:669` — "platform service: create, list, and delete the platform's AI connections". Capability/application/adapter boundary: catches authorization, persistence, approval, typed transport failure or publication-contract regressions exercised by this exact scenario. A mocked network is transport isolation, not proof of server delivery.
- **R** `tests/capabilities.test.ts:710` — "vault drive: a case carries its client data and folders stay inside their own case". Capability/application/adapter boundary: catches authorization, persistence, approval, typed transport failure or publication-contract regressions exercised by this exact scenario. A mocked network is transport isolation, not proof of server delivery.
- **R** `tests/capabilities.test.ts:750` — "knowledge engine: an empty scope searches this office's Cofre and never another's". Capability/application/adapter boundary: catches authorization, persistence, approval, typed transport failure or publication-contract regressions exercised by this exact scenario. A mocked network is transport isolation, not proof of server delivery.

## tests/chat-attachments.test.ts
- **R** `tests/chat-attachments.test.ts:15` — 'chat files persist with their message, remain private and never enter the Vault'. File upload-to-model boundary: prevents private-file leakage, repeated claims, unsupported images, chat-to-vault mixing, OCR failure misreporting, missed Word images, size limits and over-budget prompt failures.
- **R** `tests/chat-attachments.test.ts:47` — 'chat files (LUME-N): a scanned PDF on Workers explains the OCR route and is not reported as a failure'. File upload-to-model boundary: prevents private-file leakage, repeated claims, unsupported images, chat-to-vault mixing, OCR failure misreporting, missed Word images, size limits and over-budget prompt failures.
- **R** `tests/chat-attachments.test.ts:73` — 'chat files: documents up to 25 MB, images up to 10 MB'. File upload-to-model boundary: prevents private-file leakage, repeated claims, unsupported images, chat-to-vault mixing, OCR failure misreporting, missed Word images, size limits and over-budget prompt failures.
- **R** `tests/chat-attachments.test.ts:95` — 'chat files: a Word document made of pictures reaches the model as those pictures'. File upload-to-model boundary: prevents private-file leakage, repeated claims, unsupported images, chat-to-vault mixing, OCR failure misreporting, missed Word images, size limits and over-budget prompt failures.
- **R** `tests/chat-attachments.test.ts:117` — 'chat files: long documents fit, and an overfull history drops its oldest file instead of failing'. File upload-to-model boundary: prevents private-file leakage, repeated claims, unsupported images, chat-to-vault mixing, OCR failure misreporting, missed Word images, size limits and over-budget prompt failures.

## tests/chat-contract.test.ts
- **R** `tests/chat-contract.test.ts:11` — 'chat accepts the transport request without a selected case'. Real chat transport schema boundary: null case is a known real request; malformed or missing input must fail instead of crashing the route. Two cases own different accepted/rejected shapes.
- **R** `tests/chat-contract.test.ts:16` — 'chat preserves a selected case and rejects malformed context'. Real chat transport schema boundary: null case is a known real request; malformed or missing input must fail instead of crashing the route. Two cases own different accepted/rejected shapes.

## tests/citations.test.ts
- **C** `tests/citations.test.ts:22` — 'citations: candidates cover statutes, precedents, cases and CNJ numbers, without trailing punctuation'. Candidate evidence and exact keeper in the layer plan below.
- **R** `tests/citations.test.ts:35` — 'citations: a source matches on the main number and the named code or court'. Citation review/source boundary: prevents wrong-law matching, incorrect support thresholds, fabricated verified status, loss of disabled fallback and source-owner leakage. Model answers are controlled inputs; policy assertions remain code-owned.
- **R** `tests/citations.test.ts:52` — 'citations: verdicts follow the thresholds; a confident mention is dropped'. Citation review/source boundary: prevents wrong-law matching, incorrect support thresholds, fabricated verified status, loss of disabled fallback and source-owner leakage. Model answers are controlled inputs; policy assertions remain code-owned.
- **R** `tests/citations.test.ts:76` — 'citations: Jev decides what is a citation and whether the consulted source backs it'. Citation review/source boundary: prevents wrong-law matching, incorrect support thresholds, fabricated verified status, loss of disabled fallback and source-owner leakage. Model answers are controlled inputs; policy assertions remain code-owned.
- **R** `tests/citations.test.ts:129` — 'citations: sources belong to their conversation and owner'. Citation review/source boundary: prevents wrong-law matching, incorrect support thresholds, fabricated verified status, loss of disabled fallback and source-owner leakage. Model answers are controlled inputs; policy assertions remain code-owned.

## tests/conversation-time.test.ts
- **R** `tests/conversation-time.test.ts:8` — 'conversation timestamps identify the same instant in every browser timezone'. Rendered timestamp contract: catches offset misinterpretation and wrong recent/minute/invalid labels; fixed non-UTC environment avoids vacuous timezone proof.
- **R** `tests/conversation-time.test.ts:15` — 'conversation age preserves recent, minute and invalid timestamp labels'. Rendered timestamp contract: catches offset misinterpretation and wrong recent/minute/invalid labels; fixed non-UTC environment avoids vacuous timezone proof.

## tests/document-extraction.test.ts
- **R** `tests/document-extraction.test.ts:8` — 'scanned PDF uses matching native PDF.js and OCR versions, then resumes from its checkpoint'. Real PDF/DOCX OCR boundary: catches incompatible PDF.js worker versions, repeated OCR despite checkpoints, missing embedded images and unreadable oversized scans. Pixel-budget helper retained separately: successful OCR alone does not prove bounded allocation.
- **R** `tests/document-extraction.test.ts:24` — 'a Word guide made of screenshots is read by OCR in the Cofre, once, and only there'. Real PDF/DOCX OCR boundary: catches incompatible PDF.js worker versions, repeated OCR despite checkpoints, missing embedded images and unreadable oversized scans. Pixel-budget helper retained separately: successful OCR alone does not prove bounded allocation.
- **R** `tests/document-extraction.test.ts:54` — 'a phone scan with oversized pages is read at a reduced scale instead of refused'. Real PDF/DOCX OCR boundary: catches incompatible PDF.js worker versions, repeated OCR despite checkpoints, missing embedded images and unreadable oversized scans. Pixel-budget helper retained separately: successful OCR alone does not prove bounded allocation.
- **R** `tests/document-extraction.test.ts:78` — 'the OCR scale keeps ordinary pages at 1.5x and shrinks oversized ones to the pixel budget'. Real PDF/DOCX OCR boundary: catches incompatible PDF.js worker versions, repeated OCR despite checkpoints, missing embedded images and unreadable oversized scans. Pixel-budget helper retained separately: successful OCR alone does not prove bounded allocation.

## tests/document-panel.test.ts
- **R** `tests/document-panel.test.ts:18` — 'document panel: only reworded or new blocks are marked, nested ones included'. Editor block-range and split-size contract: catches incorrect highlights for nested/unchanged blocks and out-of-bounds divider values. No stronger automatic UI range/bounds assertion exists.
- **R** `tests/document-panel.test.ts:29` — 'document panel: the conversation share stays between its bounds'. Editor block-range and split-size contract: catches incorrect highlights for nested/unchanged blocks and out-of-bounds divider values. No stronger automatic UI range/bounds assertion exists.

## tests/documents.test.ts
- **R** `tests/documents.test.ts:16` — "policy: legal citation candidates, literal quotes and invented authorities". Composed/serialized document boundary: catches unsupported authorities, evidence loss, invalid dates, wrong event grouping, leaked old-template content and invalid Word formatting. Intermediate helpers eligible only after unique assertions transfer.
- **C** `tests/documents.test.ts:36` — "composition: readable labels, dates and similarity helpers". Candidate evidence and exact keeper in the layer plan below.
- **R** `tests/documents.test.ts:49` — "composition: chronology orders events, exposes divergences and gaps with readable sources". Composed/serialized document boundary: catches unsupported authorities, evidence loss, invalid dates, wrong event grouping, leaked old-template content and invalid Word formatting. Intermediate helpers eligible only after unique assertions transfer.
- **R** `tests/documents.test.ts:88` — "composition: compatible dates consolidate, uncited sources stay out, model review is index-only". Composed/serialized document boundary: catches unsupported authorities, evidence loss, invalid dates, wrong event grouping, leaked old-template content and invalid Word formatting. Intermediate helpers eligible only after unique assertions transfer.
- **R** `tests/documents.test.ts:107` — "composition: chronology blocks invented legal mentions and markdown injection". Composed/serialized document boundary: catches unsupported authorities, evidence loss, invalid dates, wrong event grouping, leaked old-template content and invalid Word formatting. Intermediate helpers eligible only after unique assertions transfer.
- **R** `tests/documents.test.ts:118` — "composition: draft paragraphs require verifiable evidence; missing references become pending". Composed/serialized document boundary: catches unsupported authorities, evidence loss, invalid dates, wrong event grouping, leaked old-template content and invalid Word formatting. Intermediate helpers eligible only after unique assertions transfer.
- **C** `tests/documents.test.ts:154` — "export: markdown tokens become Word-neutral blocks". Candidate evidence and exact keeper in the layer plan below.
- **R** `tests/documents.test.ts:164` — "export: docx library path renders bold runs and lists without literal markdown". Composed/serialized document boundary: catches unsupported authorities, evidence loss, invalid dates, wrong event grouping, leaked old-template content and invalid Word formatting. Intermediate helpers eligible only after unique assertions transfer.
- **R** `tests/documents.test.ts:175` — "export: template path keeps header and separators, drops prior body, comments, notes and metadata". Composed/serialized document boundary: catches unsupported authorities, evidence loss, invalid dates, wrong event grouping, leaked old-template content and invalid Word formatting. Intermediate helpers eligible only after unique assertions transfer.
- **R** `tests/documents.test.ts:207` — "export: editor typography comes from the template's body paragraphs and page". Composed/serialized document boundary: catches unsupported authorities, evidence loss, invalid dates, wrong event grouping, leaked old-template content and invalid Word formatting. Intermediate helpers eligible only after unique assertions transfer.
- **R** `tests/documents.test.ts:220` — "export: Markdown tables become Word tables on both paths, header row repeating". Composed/serialized document boundary: catches unsupported authorities, evidence loss, invalid dates, wrong event grouping, leaked old-template content and invalid Word formatting. Intermediate helpers eligible only after unique assertions transfer.

## tests/feedback-tickets.test.ts
- **R** `tests/feedback-tickets.test.ts:48` — 'feedback: any member reports, authors see only their own tickets, invalid input is refused'. Ticket and triage persistence boundary: catches unauthorized access, dropped reported labels, wrong priority policy, retry/admin precedence, stale updates and misdirected resolution notification.
- **R** `tests/feedback-tickets.test.ts:66` — 'feedback: the kind and place the person chose are kept apart from triage and seed the queue'. Ticket and triage persistence boundary: catches unauthorized access, dropped reported labels, wrong priority policy, retry/admin precedence, stale updates and misdirected resolution notification.
- **R** `tests/feedback-tickets.test.ts:83` — 'feedback triage: priority is composed in code from typed answers'. Ticket and triage persistence boundary: catches unauthorized access, dropped reported labels, wrong priority policy, retry/admin precedence, stale updates and misdirected resolution notification.
- **R** `tests/feedback-tickets.test.ts:107` — 'feedback triage: disabled, classified, retried and never overriding an admin correction'. Ticket and triage persistence boundary: catches unauthorized access, dropped reported labels, wrong priority policy, retry/admin precedence, stale updates and misdirected resolution notification.
- **R** `tests/feedback-tickets.test.ts:152` — 'feedback admin: platform-only, versioned updates, history and a notification on resolution'. Ticket and triage persistence boundary: catches unauthorized access, dropped reported labels, wrong priority policy, retry/admin precedence, stale updates and misdirected resolution notification.

## tests/feedback.test.ts
- **R** `tests/feedback.test.ts:21` — 'feedback hides names/metrics before voting, keeps order and allows reviewer personal feedback'. Blind-evaluation/dataset boundary: catches identity leakage, duplicate votes, forged inputs, path traversal, unfair allocation, missing consent and fabricated training readiness. Stable allocation statistical coverage has no other owner.
- **R** `tests/feedback.test.ts:41` — 'feedback concurrent duplicates preserve the first blind vote and never inflate counts'. Blind-evaluation/dataset boundary: catches identity leakage, duplicate votes, forged inputs, path traversal, unfair allocation, missing consent and fabricated training readiness. Stable allocation statistical coverage has no other owner.
- **R** `tests/feedback.test.ts:52` — 'feedback isolates offices/users and rejects revoked memberships and spoofed fields'. Blind-evaluation/dataset boundary: catches identity leakage, duplicate votes, forged inputs, path traversal, unfair allocation, missing consent and fabricated training readiness. Stable allocation statistical coverage has no other owner.
- **R** `tests/feedback.test.ts:68` — 'feedback downloads match real artifacts, keep blind filenames and reject traversal'. Blind-evaluation/dataset boundary: catches identity leakage, duplicate votes, forged inputs, path traversal, unfair allocation, missing consent and fabricated training readiness. Stable allocation statistical coverage has no other owner.
- **R** `tests/feedback.test.ts:85` — 'three-model allocation covers all pairs and both orders without showing a third response'. Blind-evaluation/dataset boundary: catches identity leakage, duplicate votes, forged inputs, path traversal, unfair allocation, missing consent and fabricated training readiness. Stable allocation statistical coverage has no other owner.
- **R** `tests/feedback.test.ts:97` — 'dataset export requires platform access and consent; preferences are not approved SFT examples'. Blind-evaluation/dataset boundary: catches identity leakage, duplicate votes, forged inputs, path traversal, unfair allocation, missing consent and fabricated training readiness. Stable allocation statistical coverage has no other owner.
- **R** `tests/feedback.test.ts:124` — 'ties remain ties and prior-round exposure is recorded in the dataset'. Blind-evaluation/dataset boundary: catches identity leakage, duplicate votes, forged inputs, path traversal, unfair allocation, missing consent and fabricated training readiness. Stable allocation statistical coverage has no other owner.
- **R** `tests/feedback.test.ts:137` — 'consent metadata is stored at submission, preserved on retry and exported from the record'. Blind-evaluation/dataset boundary: catches identity leakage, duplicate votes, forged inputs, path traversal, unfair allocation, missing consent and fabricated training readiness. Stable allocation statistical coverage has no other owner.

## tests/multipart-limit.test.ts
- **R** `tests/multipart-limit.test.ts:14` — 'multipart accepts a small file without requiring Content-Length'. Incoming stream boundary: catches excess reads/allocation, missing/lying length bypass, failure to cancel and wrong public error. Missing and false lengths remain two cases; neither is count padding.
- **R** `tests/multipart-limit.test.ts:22` — 'multipart rejects a declared oversized body before reading it'. Incoming stream boundary: catches excess reads/allocation, missing/lying length bypass, failure to cancel and wrong public error. Missing and false lengths remain two cases; neither is count padding.
- **R** `tests/multipart-limit.test.ts:33` — `multipart stops an oversized stream with ${declaredLength ? 'false' : 'missing'} Content-Length`. Incoming stream boundary: catches excess reads/allocation, missing/lying length bypass, failure to cancel and wrong public error. Missing and false lengths remain two cases; neither is count padding.
- **R** `tests/multipart-limit.test.ts:46` — 'multipart rejects malformed data with a public validation error'. Incoming stream boundary: catches excess reads/allocation, missing/lying length bypass, failure to cancel and wrong public error. Missing and false lengths remain two cases; neither is count padding.

## tests/openai-effort.test.ts
- **R** `tests/openai-effort.test.ts:6` — 'OpenAI requests carry xhigh through the Mastra adapter to the HTTP body'. Actual Mastra-to-HTTP body contract: catches dropped reasoning effort despite locally correct provider options; controlled rejection proves no real API call is needed.

## tests/vault-observability.test.ts
- **R** `tests/vault-observability.test.ts:8` — 'vault failures reach Sentry without exposing private error messages; expected validation stays quiet'. Public error/Sentry boundary: catches secret-bearing error text or stacks, dropped unexpected incidents and noisy expected validation; not duplicated by lower-level sanitizer tests.

## Live/QA retained inventory
- R scripts/verify-document-live.ts: live configured model creates document, real autosave persistence, page preview/DOCX export, divider keyboard actions, mobile focus/close/logout. Read in full, not executed (paid/live mutation). It does not replace deterministic format assertions.
- R scripts/verify-workspace-ui.ts: desktop/mobile chat compositor and scrolling, calendar markers and pagination, filters, errors/empty states, navigation, client editing, theme/keyboard. Read in full, not executed; controlled API fixtures validate rendering only.
- R scripts/verify-sentry.ts, scripts/verify-sentry-browser.ts, scripts/verify-sentry-cloudflare.ts: root operational lane retains live observability verification. Not executed by this lane.

## Preservation notes
- No negative baseline or live product defects found by this lane; no source mutations run during parallel baseline coverage.
- Agent stranger proof is cross-office; it does not supersede same-office colleague approval ownership in security.test.ts (identity lane notified).
- OCR helper scale test is retained: same-input expected calculation looks implementation-like but enforces memory budget. Real oversized-page OCR succeeds even if allocation is excessive, so no stronger budget keeper exists yet.
- capabilities knowledge vectors test name overclaims fusion: assertions only verify vector ranking, lexical degradation and tombstone denial. Retained for these actual behaviors; do not claim end-to-end fusion proof.
- webmcp route test proves adapter mapping/schemas and typed HTTP refusal, not server existence; retain alongside real service/route coverage. Mock returns success but does not implement schema/route selection under test.
- No removals of unique authentication, tenant isolation, public defaults, consent, source-policy, transaction/lease or Worker compatibility contracts merely to satisfy quota.

## Second read-only pass: owner-layer plan

No complete file retires. Six declarations/cases may retire: two duplicated invocations and four helper-level suites whose unique assertions first move into existing stronger scenarios. No test packing: each move attaches only assertions for the same transformation already exercised by its keeper.

### D1 — duplicate known-message truncation (1 case)

- Exact test: `tests/ai-store.test.ts:71`, "mergeHistory truncates on regenerate without duplicating the user turn".
- Actual failure: appends rather than replacing an already-stored incoming message ID, or retains later assistant messages. Its second count assertion duplicates its preceding exact ID array assertion.
- Owner/non-test caller: `src/lib/ai-store.ts:39` mergeHistory; `src/app/api/chat/route.ts:157`. Function branches only on incoming ID, never trigger or unchanged text.
- Exact stronger keeper: `tests/ai-store.test.ts:80`, "mergeHistory replaces and truncates when an earlier message is edited". Same known-ID branch, more subsequent messages and independently asserts changed content. Keep append case at line 65 for the other branch.
- History/reason: initial `a05f4a1` suite protected assistant-ui reload duplication; explicit comment mentions ActionBarPrimitive.Reload. Both reload and edit are deliberately one known-ID operation today.
- Unlocked deletion: declaration and duplicated fixture only; mergeHistory remains a real production caller seam.
- Risk/validation: low. Run `pnpm --filter @k5/web test tests/ai-store.test.ts`; mutation changing the known-ID branch to append must fail keeper. No additional assertions needed.

### D2 — duplicate idempotency replay (1 case)

- Exact test: `tests/capabilities.test.ts:341`, "idempotency: cached execution prevents duplicated writes".
- Actual failure: invokes callback twice or fails to deserialize/replay stored result for identical user/key/capability/args.
- Owner/non-test callers: `src/lib/application/idempotency-service.ts:29`; dispatcher `src/lib/agent-tools/index.ts:198` invokes it for non-Google writes with keys. Reads and writes `capability_idempotency` through the real DB.
- Exact stronger keeper: `tests/security.test.ts:244`, "idempotency: a reused key with different arguments conflicts instead of replaying". Its first three assertions already assert callback count 1 after identical replay, then test mismatched args/capability and same-office other-user isolation. The callback is not a mock of the cache; the real cache owns replay.
- History/reason: `a05f4a1` introduced basic happy-path coverage; security suite contains the same fixture operation plus meaningful collision and isolation negatives.
- Unlocked deletion: redundant declaration and unused capabilities.test.ts import of withIdempotency; production service stays.
- Risk/validation: low, contingent on retaining security keeper (identity lane notified). Run `pnpm --filter @k5/web test tests/capabilities.test.ts tests/security.test.ts`. No moved assertions.

### C1 — internal Word-neutral AST (1 case)

- Exact test: `tests/documents.test.ts:154`, "export: markdown tokens become Word-neutral blocks".
- Actual failure: incorrect parser block shape/order, lost bold/italic/break, wrong bullet/ordered marker, missing quote flag.
- Owner/non-test callers: `src/lib/document-export.ts:34`; markdownBlocks has only same-module exportDocument production caller (line 189), invoked by artifact DOCX routes. plainDocument and injectIntoTemplate consume those blocks.
- Exact keepers: `tests/documents.test.ts:164`, "export: docx library path renders bold runs and lists without literal markdown"; line 175, "export: template path keeps header and separators, drops prior body, comments, notes and metadata"; line 220 table export. They export the identical markdown fixture into real ZIP/XML.
- Carry first: add final DOCX assertions for line break, ordered-list start and quote indentation/italic treatment. Existing XML tests already cover headings, bold, italics, bullets, numbered markers, no literal markdown and escaped text. AST kind sequence itself is not public contract. In table case, remove direct markdownBlocks shape assertions only after verifying final Word row/cell contents (already most covered; ensure bold numeric 1 appears in body cell).
- History/reason: `a05f4a1` established parser plus both exporters; `dfe5f20` added tables. Extra internal representation assertion is now redundant beneath serialized public format contract.
- Unlocked deletion: unexport markdownBlocks and its test-only Run/Block types if repository-wide search confirms no external production use; remove test import. No runtime logic removed.
- Risk/validation: medium until break/start/quote assertions move. Run `pnpm --filter @k5/web test tests/documents.test.ts`. Mutation dropping break or quote must fail final XML keeper. No live paid export is necessary to replace an internal AST test with existing real file serialization.

### C2 — internal chronology helpers (1 case)

- Exact test: `tests/documents.test.ts:36`, "composition: readable labels, dates and similarity helpers".
- Actual failure: unreadable source label, invalid calendar date trusted, partial/null formatting broken, duplicate punctuation variants unmerged or separate numbered payments merged.
- Owner/non-test callers: `src/lib/document-composition.ts` readableLabel/dateInfo/similarDescriptions are used only inside composition; composeChronology and draft functions are the production owners, used by document jobs and artifact service.
- Exact keeper: `tests/documents.test.ts:49`, "composition: chronology orders events, exposes divergences and gaps with readable sources", plus line 88 compatible-date composition test. These already assert full/partial/null dates, label conversion, punctuation normalization grouping and disagreement output.
- Carry first: into existing chronology scenario add malformed 2023-02-30 producing Data a confirmar, worksheet label `planilha.xlsx — aba:Pagamentos!A1:C9` producing readable `aba Pagamentos!A1:C9`, and two otherwise-similar events with distinct numbered payments and distinct quotes/sources, asserting both descriptions remain independently present. Adjust expected reference/count/order only for genuine added examples; do not simply paste private-helper calls into keeper.
- History/reason: initial `a05f4a1` built deterministic composition with helper probes. Final composition scenarios now guard most of the same outputs at the real artifact boundary.
- Unlocked deletion: remove test imports and `export` on readableLabel/dateInfo/similarDescriptions. Keep implementations and live use unchanged.
- Risk/validation: medium until unique invalid-date and numeric-separation assertions move. Run `pnpm --filter @k5/web test tests/documents.test.ts`; mutate date validation/numeric identity and ensure keeper goes red.

### C3 — citation span helper replay (1 case)

- Exact test: `tests/citations.test.ts:22`, "citations: candidates cover statutes, precedents, cases and CNJ numbers, without trailing punctuation".
- Actual failure: missed/excess spans, punctuation in span text, lost paragraph, or dot-separated number normalization.
- Owner/non-test callers: `src/lib/citations/detect.ts` findCitationSpans/candidateSources consumed by `reviewCitations` in citations/review.ts; citationNumbers only called inside candidateSources. Raw anchor labels are not consumed by UI or verdict decisions (model kind determines authority category).
- Exact keepers: `tests/citations.test.ts:76`, "citations: Jev decides what is a citation and whether the consulted source backs it" uses IDENTICAL text fixture and exact expected four surviving texts plus one CNJ mention, disabled fallback statuses and source matches. Line 35 source candidate test uses REsp 1.234.567 and verifies normalized identity.
- Carry first: capture request state citations in existing transport fixture and assert the article paragraph begins `Requer a citação`; assert all five texts reaching model match the fixed expected list, including CNJ, because disabled status-only list does not independently verify CNJ text. No need preserve private anchor enum comparisons.
- History/reason: `badd7e8` introduced all five tests with Jev citation review. End-to-end deterministic review now makes standalone same-input detection a redundant lower layer.
- Unlocked deletion: unexport citationNumbers; remove import; findCitationSpans must stay exported (review owner and pure verdict test need it).
- Risk/validation: low after paragraph/input assertions move. Run `pnpm --filter @k5/web test tests/citations.test.ts tests/artifact-edits.test.ts`; mutate paragraphAt or span punctuation and confirm boundary fails.

### C4 — filename helper beneath generated PDF (1 case)

- Exact test: `tests/annexes.test.ts:32`, "annexes: PJe file names drop accents, cedillas and special characters".
- Actual failure: filenames retain accents/punctuation, omit zero pad or lack safe fallback for punctuation-only label.
- Owner/non-test callers: annexFileName in `src/lib/annexes.ts:30`, called only by same-module orderAnnexPlan and generateAnnexes. Public application callers invoke analyze/generate through capabilities.
- Exact keeper: `tests/annexes.test.ts:60`, "annexes: generation cuts the reviewed ranges into a new folder, scoped to the office and case" already asserts persisted result filenames for actual created PDFs, accent normalization, padding, folder scope, page count and source-page identity. Line 39 order test also asserts generated filenames.
- Carry first: in generation's reviewed labels use `  RG / CPF — cônjuge  ` for its existing two-page item and `Certidão de nascimento do filho (menor)` for another; append punctuation-only `***` item on a valid page and assert resulting numbered fallback document filename. Keep its existing second-PDF width check and authorization/invalid-range probes. No distinct branch for position 12 beyond padStart; generation positions >=10 may be included only if preserving independent multiple-digit numbering contract is required (avoid manufacturing dozens of generated files).
- History/reason: `ef87db8` introduced annex splitting and helper filename tests together. Generated-files suite is the stronger owner for filenames that the user actually receives.
- Unlocked deletion: remove annexFileName export and test import (all non-test uses internal); keep orderAnnexPlan exported since its no-network planning contract is still independently tested.
- Risk/validation: low to medium after label/fallback carry. Run `pnpm --filter @k5/web test tests/annexes.test.ts`; mutate normalization or fallback and keeper must fail.

### Plan totals and limitations

- 2 D and 4 C = maximum 6 executed cases fewer in this lane. This is a quality upper bound, not an instruction to delete six regardless of preservation review.
- 117 declarations inventoried; animation and multipart loops account for extra executed cases. No loops collapsed and no unrelated scenarios hidden inside surviving test declarations.
- All other exact tests stay R. Important false positives: OCR scale cap, provider default/catalog/modalities, prompt literal delimiters, schema/transport tests, ProseMirror actual document traversal, privacy and dataset consent are independent contracts.
- No whole production function deletion unlocked. Internal exports can shrink, while all runtime behavior stays intact. Proposed edit owner is root; this agent did not change source/tests or run paid/live tests.

## Cutover applied (pending root execution and independent preservation review)

All six candidates applied. Five test files changed: ai-store, capabilities, documents, citations, annexes. Four source files changed only to remove test-only exports: document-composition, document-export, citations/detect, annexes (six helper exports plus two internal AST types). Production line count unchanged; test LOC +43/-89, net -46; six declarations/cases removed. No new declarations, loops or packed unrelated scenarios.

Carries implemented: actual DOCX line breaks, numbering start=3, quote paragraph indentation/italics, bold first table cell; chronology output invalid date, worksheet label and separate numbered payments; exact model request citation texts and article paragraph; actual generated annex filenames with special characters and punctuation-only fallback. Numeric-event fixture intentionally has >80% token similarity so removing the numeric distinction would really merge the events; assertions anchor the description as an unquoted line to avoid passing merely because a quote still mentions it. This repairs a subtle weakness in the old short helper fixture, whose tokens were too dissimilar even without the numeric guard.

Checks: git diff --check reports no whitespace errors (only repository CRLF warnings). No tests/mutations run by lane because root owns serialized validation after all editors finish. Recommended preservation mutations: known-ID history appends; idempotency always executes; DOCX break dropped; paragraph quotation false; date validation accepts overflow; remove numeric-token distinction; citation paragraph empty; annex safe-name fallback missing. Restore source byte-for-byte after each mutation. Root should run focused suites and independent review before counting completion.
